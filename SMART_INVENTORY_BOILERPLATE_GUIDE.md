# SMART INVENTORY & ORDER MANAGEMENT SYSTEM — BOILERPLATE SETUP GUIDE

## Goal

Create a **production-level boilerplate** first using:

- Next.js
- TypeScript
- Tailwind CSS
- shadcn/ui
- Framer Motion
- Hono
- PostgreSQL
- Drizzle ORM
- NextAuth
- Vercel
- Neon

This guide is for the **manual setup phase only**.

**Important workflow rule:**

- Finish this boilerplate manually first
- Confirm everything runs correctly
- Only then start using Codex
- Codex must work **step by step**
- **One step per message**
- Do **not** move to the next step until the current step is completed and verified

---

# 1. Project strategy

We are building the project in **two phases**:

## Phase 1 — Manual boilerplate setup

You do this part yourself:

- create the app
- install dependencies
- set up folder structure
- configure Tailwind
- configure shadcn
- configure Hono
- configure Drizzle
- configure Neon connection
- configure NextAuth
- prepare base layout, theme, animation, utilities
- verify the app runs locally

## Phase 2 — Codex implementation

Only after Phase 1 is fully working:

- Codex builds features in controlled steps
- categories
- products
- orders
- stock logic
- dashboard
- activity logs
- restock queue
- deployment polish

---

# 2. Core principles for this project

## Production-level expectations

The boilerplate must be:

- clean
- modular
- typed
- scalable
- maintainable
- fast
- secure by default
- easy for Codex to understand

## UX expectations

The app should feel:

- fast
- smooth
- eye-soothing
- modern
- professional
- minimal and elegant

## UI expectations

Use:

- soft spacing
- clean cards
- subtle shadows
- consistent typography
- gentle animations
- calm colors
- responsive layout
- excellent empty/loading/error states

## Animation expectations

Use Framer Motion for:

- page transitions
- modal/dialog entrance
- card fade/slide
- dropdown motion
- dashboard micro-interactions

Do **not** over-animate.
Animations should improve polish, not reduce speed.

---

# 3. Recommended setup decisions

## Package manager

Use **pnpm**

## App type

Use **Next.js App Router**

## Styling

Use:

- Tailwind CSS
- shadcn/ui
- CSS variables
- dark/light theme support if wanted later

## API layer

Use **Hono** inside the Next.js project for structured backend route handling

## Database

Use:

- Neon PostgreSQL
- Drizzle ORM
- drizzle-kit for migrations

## Authentication

Use **NextAuth**

## Deployment

Use:

- Vercel for app deployment
- Neon for PostgreSQL

---

# 4. Create the project

Open terminal and create the project.

```bash
pnpm create next-app@latest smart-inventory-system
```

Recommended answers during setup:

- TypeScript: **Yes**
- ESLint: **Yes**
- Tailwind CSS: **Yes**
- `src/` directory: **Yes**
- App Router: **Yes**
- Turbopack: **Yes**
- Import alias: **Yes**
- Alias: `@/*`

Now enter the project:

```bash
cd smart-inventory-system
```

---

# 5. Install core dependencies

Install the main runtime dependencies:

```bash
pnpm add hono @hono/node-server @hono/zod-validator zod framer-motion next-auth drizzle-orm postgres dotenv clsx tailwind-merge lucide-react
```

Install development dependencies:

```bash
pnpm add -D drizzle-kit @types/node
```

Notes:

- `postgres` is a lightweight PostgreSQL client commonly used with Drizzle
- `zod` will be used for validation
- `clsx` + `tailwind-merge` help create clean `cn()` utilities
- `lucide-react` gives clean icon support
- `framer-motion` will handle animations
- `dotenv` is useful for local environment handling if needed

---

# 6. Initialize shadcn/ui

Run:

```bash
pnpm dlx shadcn@latest init
```

Recommended choices:

- style: **default** or **new-york**  
  Use **new-york** if you want a cleaner premium feel
- base color: **slate**
- CSS variables: **Yes**
- components path: default
- utils path: default

Then install initial UI components:

```bash
pnpm dlx shadcn@latest add button card input label form dialog dropdown-menu table badge tabs sheet separator skeleton sonner select textarea
```

These are enough for the boilerplate phase.

Later you can add:

- chart
- calendar
- popover
- command
- alert-dialog
- pagination
- tooltip

---

# 7. Install theme and helper libraries

Add the theme package:

```bash
pnpm add next-themes
```

You can support theme switching now or keep it prepared for later.
Even if you do not expose dark mode yet, it is fine to structure the project to support it cleanly.

---

# 8. Clean the starter app

Delete or simplify starter files generated by Next.js.

At minimum:

- clean `src/app/page.tsx`
- remove starter logos and example content
- keep a very simple landing page or redirect page

Create a clean initial homepage that confirms:

- app is running
- styling works
- fonts load
- component system works

---

# 9. Create production-friendly folder structure

Inside `src/`, prepare this structure:

```txt
src/
  app/
    (public)/
      page.tsx
    (auth)/
      login/
        page.tsx
    dashboard/
      page.tsx
    api/
      [[...route]]/
        route.ts
    globals.css
    layout.tsx

  components/
    ui/
    common/
    layout/
    animations/
    auth/
    dashboard/

  server/
    auth/
    db/
    hono/
    services/
    validations/
    repositories/

  lib/
    env.ts
    utils.ts
    auth.ts
    constants.ts

  hooks/
    use-mounted.ts

  types/
    index.ts
```

Why this structure:

- `app/` handles routing
- `components/` keeps reusable UI
- `server/` keeps backend logic separate
- `lib/` keeps helpers and shared configuration
- `hooks/` keeps reusable hooks
- `types/` keeps shared typing

This gives Codex a clear structure later.

---

# 10. Set up utility function `cn()`

Create `src/lib/utils.ts` if it does not already exist.

Use this standard utility:

```ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

This is important for clean shadcn usage.

---

# 11. Set up environment variables

Create `.env.local`

```env
DATABASE_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true
```

Later you may add:

```env
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_ID=
GITHUB_SECRET=
```

For now, the most important ones are:

- `DATABASE_URL`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`

Generate a strong secret:

```bash
openssl rand -base64 32
```

Paste that into `NEXTAUTH_SECRET`.

---

# 12. Set up Neon database

## Step 1

Create a Neon account.

## Step 2

Create a new project/database.

## Step 3

Copy the connection string.

It will look similar to:

```env
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
```

Paste it into `.env.local`

---

# 13. Configure Drizzle

Create `drizzle.config.ts` in the project root:

```ts
import { defineConfig } from "drizzle-kit";

export default defineConfig({
  schema: "./src/server/db/schema.ts",
  out: "./drizzle",
  dialect: "postgresql",
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});
```

---

# 14. Create Drizzle database connection

Create:

`src/server/db/index.ts`

```ts
import "server-only";
import { drizzle } from "drizzle-orm/postgres-js";
import postgres from "postgres";

const connectionString = process.env.DATABASE_URL!;

const client = postgres(connectionString, {
  prepare: false,
});

export const db = drizzle(client);
```

Why `prepare: false`:
It is commonly used for compatibility in serverless and Neon setups.

---

# 15. Create initial schema

Create:

`src/server/db/schema.ts`

Start with a minimal schema first.
Do not build the full competition schema yet.
Just create a tiny test table so the boilerplate can verify DB connectivity.

Example:

```ts
import { pgTable, text, timestamp, uuid } from "drizzle-orm/pg-core";

export const testItems = pgTable("test_items", {
  id: uuid("id").defaultRandom().primaryKey(),
  name: text("name").notNull(),
  createdAt: timestamp("created_at", { withTimezone: true })
    .defaultNow()
    .notNull(),
});
```

This is only for setup verification.
Later Codex will replace or extend this with the real schema.

---

# 16. Add migration scripts

Open `package.json` and add scripts like these:

```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  }
}
```

Then run:

```bash
pnpm db:generate
pnpm db:push
```

If successful, your database connection is working.

---

# 17. Create typed environment helper

Create `src/lib/env.ts`

```ts
import { z } from "zod";

const envSchema = z.object({
  DATABASE_URL: z.string().min(1),
  NEXTAUTH_SECRET: z.string().min(1),
  NEXTAUTH_URL: z.string().min(1),
  AUTH_TRUST_HOST: z.string().optional(),
});

export const env = envSchema.parse({
  DATABASE_URL: process.env.DATABASE_URL,
  NEXTAUTH_SECRET: process.env.NEXTAUTH_SECRET,
  NEXTAUTH_URL: process.env.NEXTAUTH_URL,
  AUTH_TRUST_HOST: process.env.AUTH_TRUST_HOST,
});
```

This helps fail early if env vars are missing.

---

# 18. Set up NextAuth

## Important note

NextAuth configuration depends on version.
For a modern setup in current Next.js apps, use the current official pattern for your installed version.

For boilerplate, prepare the structure first.

Create:

`src/lib/auth.ts`

Recommended structure:

```ts
import NextAuth from "next-auth";
import Credentials from "next-auth/providers/credentials";

export const { handlers, auth, signIn, signOut } = NextAuth({
  providers: [
    Credentials({
      credentials: {
        email: {},
        password: {},
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) {
          return null;
        }

        if (
          credentials.email === "demo@example.com" &&
          credentials.password === "demo123456"
        ) {
          return {
            id: "demo-user",
            name: "Demo User",
            email: "demo@example.com",
          };
        }

        return null;
      },
    }),
  ],
  session: {
    strategy: "jwt",
  },
  pages: {
    signIn: "/login",
  },
});
```

This is enough for **initial auth wiring**.
Later you can replace demo-only credentials with database users.

---

# 19. Add NextAuth route handler

Create:

`src/app/api/auth/[...nextauth]/route.ts`

```ts
import { handlers } from "@/lib/auth";

export const { GET, POST } = handlers;
```

This enables the auth routes.

---

# 20. Create session provider

Create:

`src/components/common/session-provider.tsx`

```tsx
"use client";

import { SessionProvider } from "next-auth/react";

type Props = {
  children: React.ReactNode;
};

export function AppSessionProvider({ children }: Props) {
  return <SessionProvider>{children}</SessionProvider>;
}
```

Then wrap your app in `layout.tsx`.

---

# 21. Prepare root layout

Update `src/app/layout.tsx`

Recommended responsibilities:

- import global styles
- set metadata
- wrap providers
- set base font
- keep body clean and smooth

Example structure:

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { AppSessionProvider } from "@/components/common/session-provider";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const metadata: Metadata = {
  title: "Smart Inventory System",
  description: "Production-ready inventory and order management system",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} min-h-screen bg-background font-sans text-foreground antialiased`}
      >
        <AppSessionProvider>{children}</AppSessionProvider>
      </body>
    </html>
  );
}
```

---

# 22. Configure global styling for a calm premium look

In `src/app/globals.css`, keep the base clean.

Your direction should be:

- soft background
- balanced text contrast
- clean radius
- elegant spacing
- no noisy design
- good readability

At this stage:

- keep the shadcn CSS variables
- avoid too many custom overrides
- define only the minimum project-wide improvements

Recommended visual direction:

- base background: soft neutral
- foreground: strong but not harsh
- cards: clean with light border
- radius: medium to large
- shadows: soft and subtle

---

# 23. Add Hono API entry

Create:

`src/app/api/[[...route]]/route.ts`

Use Hono as a structured API layer.

Example:

```ts
import { Hono } from "hono";
import { handle } from "hono/vercel";

export const runtime = "nodejs";

const app = new Hono().basePath("/api");

app.get("/health", (c) => {
  return c.json({
    ok: true,
    message: "API is healthy",
  });
});

const handler = handle(app);

export {
  handler as GET,
  handler as POST,
  handler as PUT,
  handler as PATCH,
  handler as DELETE,
};
```

Now you can test:

`/api/health`

Why this is useful:

- keeps backend routes cleaner
- scales better than messy route handlers
- easy to organize later

---

# 24. Add a DB test helper route later if needed

After Hono works, you can later add:

- `/api/health`
- `/api/db-check`

But only add minimal verification during boilerplate.
Do not build actual modules yet.

---

# 25. Create a beautiful base UI page

Create a simple landing page or redirect page in:
`src/app/(public)/page.tsx`

This page should confirm:

- app styling works
- shadcn works
- Framer Motion works
- layout spacing looks good

It can include:

- app title
- short subtitle
- one animated card
- one button to login
- one badge showing system status

This is not the final app.
It is only a clean verification screen.

---

# 26. Add Framer Motion foundation

Create:
`src/components/animations/fade-up.tsx`

Example:

```tsx
"use client";

import { motion } from "framer-motion";

type Props = {
  children: React.ReactNode;
  delay?: number;
  className?: string;
};

export function FadeUp({ children, delay = 0, className }: Props) {
  return (
    <motion.div
      initial={{ opacity: 0, y: 12 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4, delay, ease: "easeOut" }}
      className={className}
    >
      {children}
    </motion.div>
  );
}
```

Purpose:

- establish reusable animation patterns early
- keep motion consistent across app
- avoid random animation code later

Do not build many animations yet.
Just prepare the foundation.

---

# 27. Add Sonner toaster

In root layout or provider area, wire `sonner`.

This gives:

- clean toast messages
- production-friendly feedback
- better UX later for auth/forms/orders

---

# 28. Prepare auth pages

Create:

- `src/app/(auth)/login/page.tsx`

For boilerplate, build:

- email input
- password input
- login button
- demo login button
- calm card-based UI
- no actual full DB auth flow yet unless you want it now

The goal is:

- verify NextAuth credentials flow works
- verify session handling works
- verify protected routes can be added

---

# 29. Prepare a protected dashboard page

Create:

- `src/app/dashboard/page.tsx`

Initially this can be simple:

- welcome heading
- session user info
- logout button
- 2–3 metric placeholder cards
- smooth entry animation

Purpose:

- verify auth protection pattern
- verify dashboard shell
- confirm app structure is ready for real features

---

# 30. Add route protection pattern

Use a server-side session check pattern for protected routes.

For example:

- if no session, redirect to `/login`
- if session exists, render dashboard

Keep auth protection production-oriented from the beginning.

---

# 31. Add constants and shared types

Create:

- `src/lib/constants.ts`
- `src/types/index.ts`

For now, store things like:

- app name
- route paths
- demo login credentials constant for local development only

This keeps hardcoded strings controlled.

---

# 32. Add a mounted hook for client-only behavior

Create:
`src/hooks/use-mounted.ts`

```ts
"use client";

import { useEffect, useState } from "react";

export function useMounted() {
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setMounted(true);
  }, []);

  return mounted;
}
```

Useful later for theme or client-only rendering issues.

---

# 33. Verify everything locally

## App checks

- `pnpm dev` runs
- homepage loads
- no obvious TypeScript errors
- no module resolution issues

## Styling checks

- Tailwind works
- shadcn components render correctly
- fonts work
- spacing looks clean

## Animation checks

- Framer Motion animation renders smoothly
- no hydration issues

## Database checks

- Neon connection works
- Drizzle push/generate works
- schema applied successfully

## Auth checks

- login page loads
- demo login works
- dashboard can be protected
- logout works

## API checks

- `/api/health` works
- Hono routing works

Do not move forward until all of these are working.

---

# 34. Create a strict step-by-step Codex workflow

Once boilerplate is complete, Codex must follow this rule:

## Rule

**One step per message**

- do one task
- wait
- verify
- only then continue

## Do not allow Codex to:

- build the whole app in one shot
- change unrelated files unnecessarily
- refactor everything at once
- skip validation
- jump ahead

## Every Codex step should:

- mention exactly which files are touched
- explain what is being added
- keep code production-level
- preserve existing structure
- avoid breaking previous setup

---

# 35. Recommended Codex implementation sequence after boilerplate

Only after manual setup is complete:

## Step 1

Create final database schema:

- users
- categories
- products
- orders
- order_items
- restock_queue
- activity_logs

## Step 2

Create Drizzle relations and migration

## Step 3

Replace demo auth with real DB-backed credentials auth

## Step 4

Build dashboard shell and navigation

## Step 5

Build categories CRUD

## Step 6

Build products CRUD

## Step 7

Build order creation flow

## Step 8

Implement stock deduction transaction logic

## Step 9

Implement restock queue logic

## Step 10

Implement activity logs

## Step 11

Add filters/search/pagination

## Step 12

Add dashboard metrics and charts

## Step 13

UI polish, loading states, empty states, toasts

## Step 14

Deployment hardening

---

# 36. Suggested first manual milestone

Before Codex starts, your first milestone should be:

- project created
- dependencies installed
- shadcn initialized
- env created
- Neon connected
- Drizzle configured
- test migration pushed
- Hono health route working
- NextAuth demo login working
- login page + protected dashboard working
- Framer Motion working
- app runs cleanly

If all of these are done, your boilerplate is ready.

---

# 37. Recommended quality standards before Codex begins

Before moving to feature development, make sure:

## Code quality

- strict TypeScript
- no any unless truly necessary
- no dead starter code
- no giant mixed-purpose files
- clean import structure

## UX quality

- page loads quickly
- no layout shift
- no ugly starter UI
- clean loading and hover states

## Architecture quality

- backend logic not mixed randomly into UI files
- reusable helpers separated
- auth separated
- DB separated
- route structure understandable

If this is clean now, Codex will perform much better.

---

# 38. Final instruction for the workflow

After completing this manual guide:

1. verify all setup is working
2. commit the boilerplate to Git
3. then start Codex
4. Codex gets **one task only**
5. after each task, test locally
6. only then request the next step

This will prevent messy code and keep the project competition-ready.

---

# 39. Suggested first Git commits

Recommended commit sequence:

```bash
git init
git add .
git commit -m "chore: initialize Next.js inventory project"
```

After dependency and config setup:

```bash
git add .
git commit -m "chore: set up ui, auth, hono, drizzle, and database boilerplate"
```

After login and dashboard verification:

```bash
git add .
git commit -m "feat: add initial auth flow and protected dashboard shell"
```

These checkpoints are useful before Codex starts editing files.

---

# 40. What not to do right now

Do **not** build these yet during boilerplate:

- full product CRUD
- full order logic
- final DB schema
- charts
- role permissions
- heavy dashboard widgets
- advanced filters
- seed scripts unless necessary

Right now the goal is only:
**stable, beautiful, production-level boilerplate**

---

# 41. Boilerplate completion checklist

Use this checklist before moving to Codex:

- [ ] Next.js project created with TypeScript and App Router
- [ ] Tailwind configured
- [ ] shadcn initialized
- [ ] base UI components installed
- [ ] Framer Motion installed and tested
- [ ] Hono installed and `/api/health` working
- [ ] Neon PostgreSQL created
- [ ] `.env.local` configured
- [ ] Drizzle configured
- [ ] initial schema created
- [ ] DB push/generate successful
- [ ] NextAuth configured
- [ ] demo credentials login working
- [ ] session provider connected
- [ ] protected dashboard page created
- [ ] logout works
- [ ] root layout cleaned
- [ ] calm premium base styling applied
- [ ] folder structure organized
- [ ] no obvious TypeScript errors
- [ ] initial Git commits done

Only after every important box is done should Codex begin feature development.

---

# 42. Final note

This setup phase will decide how clean and scalable the whole project becomes.

Do this part carefully.
Once this foundation is correct, Codex can be used much more effectively and safely.

The best workflow is:

- manual foundation first
- verify everything
- then Codex
- one step per message
- production-level code only
